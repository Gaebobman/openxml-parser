# example (local only)

This directory is **gitignored**. Place proprietary PPTX files and generated outputs here for local development only.

Do not commit real customer or internal documents to the repository.

**Do not reference `example/` or `testdata/` file names in README, docs, or commit messages.**

For shareable demos, use [`public_samples/`](../public_samples/) instead.
