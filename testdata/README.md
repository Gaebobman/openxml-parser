# testdata (local only)

This directory is **gitignored**. Use it for local fixtures only:

- `pptx_samples/` — real PPTX files for integration tests (`RUN_REAL_PPTX_TESTS=1`)
- `golden/` — golden label JSON files for regression (`*.golden.json`)

Copy or generate these on your machine; they are not distributed via git.
